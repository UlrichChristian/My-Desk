## 08:31 — Financial app: run from worktree + Deploy B vintaging

Ran My Desk from this worktree (`python c.py`). Port 5002 is still the old
`C:\Christian` process (integrations 404 there), so the worktree app is on
**http://127.0.0.1:5003**. Sign in as culrich (blank password). No `.env` /
`FIN_APP_DIR` override — launch from the worktree instead.

**Deploy B applied** to this checkout's `financial.db`: deleted 11 months of
`source=estimated` `account_period_metrics` (2025-08 through 2026-06). July and
August 2026 remain. Aug 2025 Revenue/Premium is blank; Aug 2026 still
`$432,925.39` with premium. `load_import(..., metrics_mode="latest")` is the
live path; `migrate_schema()` re-applies the drop so it sticks. Parity tests
still use `all_periods`. One-shot: `scripts/apply_vintaging.py`.

**Leftover from Deploy A, triaged:**
- Done here: Reference Review (`/financial/reference-review`); Process Street
  to-do + audit copy on Integrations / import / review, plus MP-FIN-01..03 in
  `Knowledge App/notes/processes.md`.
- Parked (need Christian / time): drop legacy tables after a month of use;
  re-enter wiped Aug 2025 qty/rate into `ingest_corrections`; delete
  `WFSteel_Deferred_#1` in QuickBooks (already excluded in-app).
