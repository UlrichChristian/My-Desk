# 2026-06-30

## 14:00 — Orphaned recurring task fix (Focus Board)
Task #90 (`Manually Post GST/HST for EIC Accounts`) was invisible: saved as `is_recurring=1` under active parent #38
(Month End Tasks on matrix) instead of recurring template root #36, with no Schedule flags (`important/urgent/placed` all 0).
Recurring templates never show on the matrix; Recurring section only lists subtasks of recurring roots.

**Repair:** #90 → `parent_id=36`, `important=1`, `urgent=0`, `placed=1` (Schedule when started).

**Prevention:** `resolve_recurring_parent()` auto-redirects same-title active parent to recurring template;
`placement_from_recurring_root()` inherits quadrant from template root; quick-add shows recurring-template parent
picker when Recurring is checked; Recurring section hint added.
