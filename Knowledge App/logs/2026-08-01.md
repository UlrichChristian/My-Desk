# 2026-08-01
## 12:28 — Focus Board v2 progress
Continued type-based redesign on `focus-v2` branch (baseline `061cb2c`).
- Board UI: prominent Start-timer, ★/🔥 markers, category dots, Ad-hoc|Recurring|Project column order, subtask step counts, `return_to=v2` redirects
- Services/routes: `task_type` enrich, v2 add-task lands in Pipeline (flags ≠ placement), edit form gets important/urgent/waiting_on/type_override
- MCP: `list_active_board` → today/active/pipeline; `place_task` + `promote_to_active` promote without quadrant; `move_to_pipeline` clears placed; slim payloads use band+type
- DB backup: `focus.db.bak-20260801-122336`
Preview at `/focus/v2`; classic still at `/focus`. Cutover + `/focus/v1` alias still pending.

## 12:30 — Focus Board v2 column sizing
Renamed the user-facing Ad-hoc label to To-do. Active columns are now ordered To-do | Project | Recurring, with To-do twice the width of each narrower column. Internal type key remains dhoc for schema compatibility.

## 12:34 — Focus Board v2 compact cards and quick-add
Today cards now use three compact rows: wrapping title, one-line indicators, and actions/timer. Quick-add uses a labelled two-row grid with controls beneath labels and the Add button spanning both rows on the right.

## 12:38 — Quick-add clarified
Adjusted quick-add to exactly two visual lines overall: one shared label line and one shared control line, with Add at the far right.

## 12:45 — Focus Board v2 top-bar pivot
Replaced the large timer/inline quick-add area with a compact top bar containing timer state, task search, and one Add task button. Add task opens a modal with Task, Project, and Recurring task modes; category/classic links moved into the modal footer.

## 12:52 — Active board hierarchy pivot
Active now uses a wide To-do column with a vertically stacked Project/Recurring side column. Project groups and recurring tasks are collapsed by default. To-do is split into Urgent / important and Schedule; urgent, important, or due/overdue items automatically render in the top section without changing stored flags. Search temporarily expands matching collapsed groups.

## 12:58 - To-do section rename + stale server found
Renamed the To-do priority section label from Urgent / important to Current.
Diagnosed the empty-section report: it was not a code bug. Three duplicate My Desk processes were bound to port 5002, and the one actually answering predated the v2 work (it returned 404 for /focus/v2). Stopped the three stale listeners and started a single fresh instance. Test-client render confirms 28 tiles in Current and 3 in Schedule.

## 13:05 - Vertical Eisenhower To-do lists
Replaced Current/Schedule with four vertical To-do sections: Do Now, Schedule, Delegate, Later. Due/overdue tasks promote to Do Now without changing stored flags. Each section is capped at roughly eight visible tiles and scrolls beyond that. Dragging between quadrants updates important/urgent flags. Live render counts: Do Now 8, Schedule 16, Delegate 4, Later 3.

## 13:07 - To-do header colour
Changed the v2 To-do header from orange to #39393c (PMS 447C).

## 13:39 - Pipeline and project management sections
Restyled Pipeline as a collapsed recurring-template-style section. Added a collapsed Projects management section with completion counts and 3-dot links to a new project editor for name, description, and archive status.

## 13:53 — Project/Recurring group drag-reorder
Project and Recurring Active groups on v2 can now be drag-reordered via a handle on each group header (same pattern as To-do tiles). Persists with `/focus/projects/reorder` and `/focus/tasks/reorder`.

## 14:02 — Move-to-Today arrow + type lock
Replaced the ring-dot Active control with a right-pointing arrow (straight shaft, curved tip). Project/Recurring can no longer be dropped into To-do; → (or drag to Today) sends them straight to Today.

## 14:06 — My Desk logo
Replaced the header’s placeholder SVG swoosh with the supplied Effortless Admin PNG logo.

## 14:20 — Focus Board cutover
Promoted the redesigned type-based board to the sole `/focus` experience. Removed
the classic route/template, preview endpoint, compatibility redirects/fields,
classic placement/order APIs, obsolete matrix query, and dead classic-only CSS.
Updated Focus Board context. Smoke test: `/focus` renders 200; `/focus/v2` is 404.

## 14:32 — Project completion %
Added manual `projects.completion_pct` (0–100). Editable on the project form;
shown just left of the ⋮ on Active project groups and Projects management.
