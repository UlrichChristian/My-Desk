# 2026-09-22

## 09:40 — Removed Focus Board Pipeline holding area

New tasks now land on the board immediately (`placed=1`): project group if a
project is set, Recurring templates if flagged recurring, otherwise Active To-do
(Later when unflagged). Existing unplaced live rows are promoted on next DB
connect. MCP `add_task` / `add_tasks` no longer send unflagged or project tasks
into a backlog. `move_to_pipeline` means Today → Active. `list_active_board`
still returns a `pipeline` key, always empty. `status='pipeline'` is unchanged
as the stored “not started” value.

## 10:20 — Dropped placed and status=pipeline

Not-started work is now `status='active'`. The `placed` column is gone — if a
task is not Today / done / archived, it is on the board. Remaining `pipeline`
rows (including Invoice Reconciliation #294) migrate to `active` on next DB
connect. MCP still accepts `pipeline` as an alias for `active`. Restart My Desk
so the running process picks up the new writes.
