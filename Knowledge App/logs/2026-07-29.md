
## 20:38 � Financial Biggest Clients status
Reviewed Monthly Biggest Clients. Phase 1 is live at `/financial/biggest-clients`. `financial.db` has 2 import batches, 6,924 revenue rows, period `2026-03` only. Top 3 match acceptance targets from the plan: New Gold 30,378.42, 123Dentist 16,595.25, Optima 13,468.17. `context.md` still describes Analysis cards as stubbed/coming soon � stale vs `SECTIONS` (`available: True`). Change Report and Cash Flow/P&L still not built.

## 20:45 � Biggest Clients advisor/premium join fix
Root cause: QBO names are `Group:SystemId` (e.g. `New Gold:Rainy River`) but PreCustomer joined the full string to account-list SYSTEM ID. Fix in `revenue_mapping.resolve_precustomer`: take text after `:`. Match rate 33k?79.5k of 79.6k rows. Re-imported latest upload (import #5); Mar 2026 top clients now have advisor + premium. Remaining ~656 nulls are mostly accounts missing broker/premium in the CSV or the ~11 NAME mismatches.

## 20:52 � Biggest Clients: stale accounts CSV + oid join
User correct that brokerList is populated in the CSV. Remaining blanks were (1) revenue upload used older `ea_accounts_all.csv` (1449 / max oid 2852) while Pull Accounts had a fresher file (1489 / max oid 2921), and (2) Join 2 was NAME-only so renames like Vibrant Community Health missed. Fixed Join 2 to prefer ACCOUNT ID?oid. Re-imported with fresher CSV (import #6): advisor nulls 656?35; only Manulife + unmapped blank at client rollup.

## 21:05 � Biggest Clients Excel export matches sample layout
Export button now writes a 3-sheet workbook modeled on `temp/202603_Revenue.xlsx`: formatted `Data` Excel Table (TableStyleMedium16, sample columns), plus formatted `Revenue by Client` and `Revenue by Advisor` pivot-style sheets (titles, Lato, number formats, grand totals, cumulative share on advisor). Live COM pivots were tried then dropped � too fragile; static sheets keep correct Revenue Share / Cumulative Share.

## 21:20 � Native Excel PivotTables restored on export
Export now builds real PivotTables (via Excel COM) from the `Data` table: Revenue by Client (Client+Advisor, tabular, sorted by Revenue) and Revenue by Advisor. Static formatted sheets remain as fallback if Excel/COM unavailable. Fixed COM bug where `Sheets.Move` was dropping Advisor/Data sheets.

## 21:30 � Monthly Change Report (Phase 2)
Shipped MoM revenue bridge at `/financial/change-report`. Service `revenue_change.py` attributes ? to New/Lost/Volume/Price/Mix/Other from drilldown qty�rate. Verified 2026-03 vs 2026-02: delta 41,079.51 ties out (residual 0). Tile enabled; Excel export Summary + By Client. Docs updated in Financial `context.md` and Phase 2 section on `BIGGEST_CLIENTS_PLAN.md`.

## 21:50 � Exclude Referral Fees:Email Feed
Manulife on Biggest Clients / Change Report was H&D referral income (`Referral Fees:Email Feed`), not a client. Excluded that income account from both report queries (still stored on ingest). Footnotes updated.

## 21:55 — Financial unit test suite
Added pytest under `Financial Management App/tests/` (in-memory SQLite). Covers mapping, Biggest Clients + referral exclusion, Change Report drivers, ingest normalize/replace-by-period. Run: `python -m pytest` from the Financial app folder (22 passed).

## 22:55 � Financial Change Report session write-up
Full session outcomes (exclusions, pytest, expand/sort/prior picker, WF Steel mapping, Aug 2025 qty/rate lump fixes) saved to `Knowledge App/logs/2026-07-29-financial-change-report-session.md`.

## 23:00 - Financial Analysis foundation session write-up
Confirmed status quo: `/financial`, `/financial/biggest-clients`, `/financial/change-report` all return 200; Biggest Clients renders 443 clients for latest period (2026-07). The earlier "doesn't navigate to a list" was a stale server started before the report route landed (data imported fine) - restart My Desk if it recurs. `financial.db` import #6 holds the full year (79,610 rows, 2025-08..2026-07, 1,489 accounts, 489 clients, 4 unmapped). Full design + build narrative (app-vs-Excel decision, Pull Accounts DOM-scrape -> JSON API pivot, all-statuses filter `0-1;0-2;0-3`, mapping Customer-vs-Name and GROUP-ID-NaN fixes, penny-exact acceptance test) saved to `Knowledge App/logs/2026-07-29-financial-analysis-foundation-session.md`.
